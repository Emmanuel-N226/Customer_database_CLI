# CRUD_apps
A simple Python-based CLI (Command-Line Interface) CRUD application using SQLite3. This project allows users to Create, Read, Update, and Delete customer records in a local database.
"""
Create a customer table and add initial data
Read/View all data, a single row, or a limited number of rows
Update first name, last name, or email of a customer
Delete a row by its ID
Robust input handling and clear CLI prompts for easy use
Fully modular code with separate database and app logic
"""
